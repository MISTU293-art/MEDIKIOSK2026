<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm mb-4">
  <div class="container-fluid px-4">
    <a class="navbar-brand fw-bold d-flex align-items-center gap-2" href="/staff/dashboard">
      <i class="bi bi-hospital fs-4"></i> MediKiosk Portal
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#portalNavbar">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="portalNavbar">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if (typeof user !== 'undefined' && user) { %>
          <% if (user.role === 'staff' || user.role === 'admin') { %>
            <li class="nav-item">
              <a class="nav-link <%= typeof title !== 'undefined' && title.includes('Staff') ? 'active' : '' %>" href="/staff/dashboard">
                <i class="bi bi-person-lines-fill me-1"></i> Reception & Triage
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link <%= typeof title !== 'undefined' && title.includes('OCR') ? 'active' : '' %>" href="/staff/ocr-verify">
                <i class="bi bi-file-earmark-medical me-1"></i> Side-by-Side OCR Review
              </a>
            </li>
          <% } %>
          <% if (user.role === 'doctor' || user.role === 'admin') { %>
            <li class="nav-item">
              <a class="nav-link <%= typeof title !== 'undefined' && title.includes('Doctor') ? 'active' : '' %>" href="/doctor/queue">
                <i class="bi bi-stethoscope me-1"></i> OPD Doctor Queue
              </a>
            </li>
          <% } %>
          <% if (user.role === 'admin') { %>
            <li class="nav-item">
              <a class="nav-link <%= typeof title !== 'undefined' && title.includes('Admin') ? 'active' : '' %>" href="/admin/dashboard">
                <i class="bi bi-speedometer2 me-1"></i> Analytics & KPIs
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link <%= typeof title !== 'undefined' && title.includes('User') ? 'active' : '' %>" href="/admin/users">
                <i class="bi bi-people me-1"></i> Users & Roles
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link <%= typeof title !== 'undefined' && title.includes('Hardware') ? 'active' : '' %>" href="/admin/kiosks">
                <i class="bi bi-display me-1"></i> Kiosk Health
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link <%= typeof title !== 'undefined' && title.includes('Compliance') ? 'active' : '' %>" href="/admin/compliance">
                <i class="bi bi-shield-check me-1"></i> Compliance & Audits
              </a>
            </li>
          <% } %>
        <% } %>
      </ul>

      <div class="d-flex align-items-center gap-3">
        <a href="/kiosk" class="btn btn-outline-light btn-sm" target="_blank">
          <i class="bi bi-display me-1"></i> Launch Kiosk UI
        </a>
        <% if (typeof user !== 'undefined' && user) { %>
          <div class="dropdown">
            <button class="btn btn-light btn-sm dropdown-toggle fw-semibold" type="button" data-bs-toggle="dropdown">
              <i class="bi bi-person-circle me-1"></i> <%= user.name %> (<%= user.role.toUpperCase() %>)
            </button>
            <ul class="dropdown-menu dropdown-menu-end shadow">
              <li><span class="dropdown-item-text text-muted small"><%= user.email %></span></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item text-danger" href="/auth/logout"><i class="bi bi-box-arrow-right me-2"></i>Logout</a></li>
            </ul>
          </div>
        <% } else { %>
          <a href="/auth/login" class="btn btn-light btn-sm fw-semibold">Login</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
